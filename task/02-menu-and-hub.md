# Фаза 2 — Меню и страница-хаб «Направления практики» ✅

**Статус:** завершена 2026-04-16. Выбран динамический вариант хаба — `layouts/_default/napravleniya.html` + partial `section-home.html`. Меню полностью перестроено: 4 пункта (Консультации, Представительство в суде, Подготовка документов, Направления практики).

**Цель:** перестроить главное меню под три типа услуг и создать страницу, агрегирующую все 8 направлений.

**Зависит от:** [01-sections-scaffold.md](01-sections-scaffold.md) (разделы должны существовать).

## Главное меню — итоговый состав

Согласно [STRUCTURE_DRAFT.md](../STRUCTURE_DRAFT.md), пункты:

1. Консультации → `/konsultaciya.html` (или текущий `yuridicheskaya-konsultaciya.html`, см. реш. 10)
2. Представительство в суде → `/predstavitelstvo-v-sude.html`
3. Подготовка документов → `/podgotovka-dokumentov.html`
4. **Направления практики** → `/napravleniya.html` (новый пункт, хаб)

Текущий пункт «Услуги» (`uslugi`) — убрать или переосмыслить (нет соответствующего контента).

## Шаги

### Страница-хаб

- [x] **2.1.** Создать `content/napravleniya.md` (standalone-страница, `uglyURLs = true` даст `/napravleniya.html`).
- [x] **2.2.** Front matter: `title = 'Направления практики'`, `description`, `showOnMain = false`.
- [x] **2.3.** Решить: статический список ссылок в теле или динамический шаблон.
  - **Статический** — проще, но требует ручной синхронизации при добавлении раздела.
  - **Динамический** — отдельный layout (`layouts/napravleniya.html` или тип страницы через `type`/`layout` в front matter), итерирующий `site.Sections`. Аналогично тому, как `home.html` работает с `showOnMain`.
- [x] **2.4.** Реализовать выбранный вариант. Если динамический — можно переиспользовать партиал `section-home.html`.

### Меню

- [x] **2.5.** В `hugo.toml`, секция `[[menu.main]]`, заменить текущие два пункта на 4 новых. Указать `weight` для порядка.
- [x] **2.6.** Проверить, что `menu.html` корректно подсвечивает активный пункт на каждой из 4 страниц.

## Критерий готовности

- В шапке видны 4 пункта меню.
- Страница `/napravleniya.html` открывается и показывает все 8 разделов со ссылками и описаниями.
- Активный пункт меню подсвечивается (CSS-класс `active` / `ancestor`).

## Риск

Если выбрали динамический вариант — при добавлении раздела без `showOnMain` он не попадёт и на хаб. Нужно либо отдельный параметр (`showOnHub`), либо итерировать по всем `site.Sections` без фильтра.
