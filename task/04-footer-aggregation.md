# Фаза 4 — Блок «Все направления» в футере ✅

**Статус:** завершена 2026-04-16. Итерация `range site.Sections.ByWeight` внутри `<nav class="footer-sections">` + скоуп-стили в `main.css` (grid с `auto-fit minmax(220px, 1fr)`).

**Цель:** добавить в футер компактный список ссылок на все 8 разделов — чтобы с любой страницы можно было перейти в соседнее направление.

**Зависит от:** [01-sections-scaffold.md](01-sections-scaffold.md) (разделы существуют).

## Шаги

- [x] **4.1.** Открыть `layouts/_partials/footer.html`.
- [x] **4.2.** Добавить блок (перед или после существующего меню футера) — итерация по `site.Sections` с выводом `.Title` и ссылки на `.RelPermalink`.
- [x] **4.3.** Решить: выводить все секции или фильтровать (например, по `showOnMain`). По умолчанию — все: футер должен быть исчерпывающим, в отличие от главной.
- [x] **4.4.** Стилизация в `assets/css/main.css`: колонка/сетка, не дублирует визуально политику/условия.
- [x] **4.5.** Проверить на узком экране (мобильный): блок переносится корректно, не ломает текущий футер.

## Критерий готовности

- В футере на любой странице виден список всех 8 разделов со ссылками.
- Ссылки ведут на корректные URL.
- Вёрстка не ломается ни на десктопе, ни на мобильном.

## Заметка

Если в [02-menu-and-hub.md](02-menu-and-hub.md) решили делать динамический хаб — можно вынести итерацию в общий партиал (`_partials/all-sections-list.html`) и переиспользовать его в футере и на хабе.
