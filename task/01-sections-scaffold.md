# Фаза 1 — Каркас разделов ✅

**Статус:** завершена 2026-04-16.

**Цель:** создать пустые `_index.md` для всех 8 разделов с корректным front matter.

**Зависит от:** [00-decisions.md](00-decisions.md) (пункты 8, 9 — схема URL).

## Разделы к созданию

Согласно [STRUCTURE_DRAFT.md](../STRUCTURE_DRAFT.md):

| # | Название | Слаг (draft) | Статус |
|---|----------|--------------|--------|
| 1 | Наследственные споры | `nasledstvo` | уже есть как `nasledstvennye_spory` — см. реш. 8 |
| 2 | Семейное право | `semejnoe-pravo` | создать |
| 3 | Автоюрист / ДТП | `avtoyurist` | создать |
| 4 | Защита прав потребителей | `zashhita-prav-potrebitelej` | уже есть как `zashhita_prav_potrebitelja` — см. реш. 9 |
| 5 | Жилищные споры | `zhilishhnye-spory` | создать |
| 6 | Земельные споры | `zemelnye-spory` | создать |
| 7 | Споры с государственными органами | `spory-s-gosorganami` | создать |
| 8 | Возмещение ущерба | `vozmeshhenie-ushherba` | создать |

## Шаги

- [x] **1.1.** Решить по существующим разделам (переименовать / оставить / обновить URL).
- [x] **1.2.** Для каждого нового раздела создать `content/<slug>/_index.md`. Если `make section` корректно работает в bash под Windows — через него; иначе вручную.
- [x] **1.3.** В каждый `_index.md` прописать front matter:
  ```toml
  +++
  title = '<Название раздела>'
  date = 2026-04-16
  draft = false
  description = '<1–2 предложения для <meta description> и карточки на главной>'
  showOnMain = true
  weight = <10, 20, 30… для порядка на главной>
  +++
  ```
- [x] **1.4.** В тело `_index.md` — короткое вводное описание раздела (2–4 абзаца, можно заготовку).
- [x] **1.5.** Проверить через `make hugo_serve`: главная показывает карточки всех разделов, каждый раздел доступен по URL.

## Критерий готовности

- В `content/` лежат 8 каталогов разделов с заполненным `_index.md`.
- На главной отображаются 8 карточек (через `section-home.html`).
- Все URL открываются без 404.

## Заметки по `weight`

Порядок разделов на главной — обсуждаемо. Пока по важности/частоте: наследство → семья → автоюрист → потребители → жильё → земля → госорганы → ущерб.
